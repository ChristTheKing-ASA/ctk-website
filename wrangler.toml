# Cloudflare Workers configuration for CTK Website
name = "ctk-website"
compatibility_date = "2024-01-01"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "ctk-website-db"
database_id = "" # Will be filled after creating database in Cloudflare dashboard

# Environment variables (set in Cloudflare dashboard for production)
# For local development, use .env.local
[vars]
# Add non-sensitive variables here if needed
